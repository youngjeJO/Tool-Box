import React from 'react';
import styled from 'styled-components';

function Header() {
  return <Title>Tool Box</Title>;
}

export default Header;

const Title = styled.div`
  font-size: 24px;
  font-weight: bold;
  text-align: center;
`;
